en:
  site_settings:
    plugin_check_email_enabled: "Enable disposable email validation via external API"
    plugin_check_email_api_url: "URL to API endpoint (must return json {'disposable':true|false}"
    plugin_check_email_debug_log: "Enable Discourse syslog error_log debugging"
